{
  "name": "@job-radar/scoring",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "eval": "node --import tsx src/eval-runner.ts"
  },
  "dependencies": {
    "@job-radar/shared": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@job-radar/testing": "workspace:*",
    "tsx": "^4.23.12"
  }
}
